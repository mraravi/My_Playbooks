---
- hosts: g23acxwas003.au.ibm.com
  user: anstadm
  become: true
  gather_facts: False
  tasks:   
    - name: change permissions on /test1
      file: path="/test1" mode=0777 owner=mrara group=staff

    - name:  upgrade all kernel packages on the server
      command: emgr -e 1022100a.210112.epkg.Z -p
