---
- hosts: g23acxwas003.au.ibm.com
  gather_facts: False
  tasks:   
    - name: create logical volume testlv
      command: mklv -t jfs2 -y testlv rootvg 8 
      args:
        creates: /dev/testlv
    
    - name: create filesystem /test
      command: crfs -v jfs2 -d /dev/testlv -m /test -A yes
      args:
        creates: /test
  
